# AI Working Rules

- After any code or documentation change, always list the exact files changed.
- For Google Apps Script projects, also say which local files need to be copied into Apps Script tabs.
- Do not tell the user to copy all files when only a subset changed.
